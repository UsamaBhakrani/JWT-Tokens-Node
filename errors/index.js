const AuthError = require("./auth-error");
const CustomAPIError = require("./custom-error");
const BadRequestError = require("./bad-request");

module.exports = { AuthError, CustomAPIError, BadRequestError };
